apiVersion: v1
kind: Service
metadata:
  name: backstage
  namespace: backstage
  labels:
    app: backstage
spec:
  type: LoadBalancer
  selector:
    app: backstage
  ports:
  - name: http
    port: 7007
    targetPort: 7007

---    
